<!DOCTYPE html>
<html>
<head>
	<style type="text/css">
		.body-head
		{
			margin: 10px; padding:10px;border-color:#CCCCCC; border-style:hidden
		}
		.body-content
		{
			text-align:center;
			padding: 50px;
		}
		.btn-small
		{
			height: 25px; padding: 0px; text-align: center; width:auto; background:#999999
		}
		.input-small
		{
			height: 10px; background:#CCCCCC
		}
	</style>
  	<title>StarkeMadchen</title>
  	<%= stylesheet_link_tag    "application", :media => "all" %>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>
  	
</head>
<body class="page-style">
	<% flash.each do |key,value| %>
		<div class="alert alert-<%= key %> body-content">
			<%= value %>
		</div>
	<% end %>
	<%= render 'layouts/header' %>
	<%= yield %>
	<%= render 'layouts/footer' %>
	<%= debug(params) if Rails.env.development? %>
</body>
</html>